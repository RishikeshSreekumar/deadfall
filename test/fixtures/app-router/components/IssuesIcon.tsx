// Used ONLY inside a config object (never directly as JSX in a component).
export function IssuesIcon({ className }: { className?: string }) {
  return <svg className={className} viewBox="0 0 16 16" />;
}
